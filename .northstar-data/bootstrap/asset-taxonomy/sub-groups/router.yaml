apiVersion: network.northstar.astrona.io/v1alpha1
kind: SubGroup
metadata:
  name: Router
  parentCategory: Network
spec:
  order: 10
  icon: i-heroicons-cpu-chip
  description: "BGP edge routers, core routers, or gateways"
